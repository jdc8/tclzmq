package require tcltest
package require zmq

tcltest::test message-1 {zmq message} -body {
    zmq message
} -returnCodes {error} -result {wrong # args: should be "zmq message name ?-size <size>? ?-data <data>?"}
